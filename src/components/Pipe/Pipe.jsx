import React from 'react';
import styles from './Pipe.module.css'
const Pipe = ({style}) => {
    return (
        <div style={style} className={styles.pipe}>
            
        </div>
    );
};

export default Pipe;